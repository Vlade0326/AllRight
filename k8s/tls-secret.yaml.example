# Self-signed TLS for Kind Ingress (dev only).
# Generate: npm run k8s:tls
apiVersion: v1
kind: Secret
metadata:
  name: allright-tls
type: kubernetes.io/tls
stringData:
  tls.crt: |
    REPLACE_WITH_CERT
  tls.key: |
    REPLACE_WITH_KEY
